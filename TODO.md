# TODO - Temple Billing System

- [x] Fix React runtime error: `AdminDashboard is not defined` in `frontend/src/App.jsx` by adding missing import.
- [ ] Fix next runtime error: `LeaveHistory is not defined` in `frontend/src/App.jsx` by either importing the correct component or updating the route to use an existing leave page.
- [ ] Run frontend build/dev to confirm there are no more undefined component references.

